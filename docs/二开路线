拆成 8 个功能模块

底层推荐继续用 **uiautomator2**。它的架构本身就是“设备端运行基于 UiAutomator 的 HTTP 服务，Python 客户端通过 HTTP 调用 Android 自动化能力”

本地 Web 服务封装。支持截图、层级 XML、点击、XPath、resourceId、text 等能力，正好匹配你的需求

第一个必须实现的是 **ADB 设备连接与检测模块**。

需要支持：

```text
1. 检测本机是否安装 adb
2. 执行 adb devices 获取设备列表
3. 判断设备状态：
   - device：可用
   - unauthorized：未授权，需要手机确认 USB 调试
   - offline：离线，需要重连
4. 单设备时自动连接
5. 多设备时支持指定 serial
6. 连接成功后初始化 uiautomator2
7. 断开后支持重新连接
```

命令设计可以这样：

```bash
android-inspector start
```

多设备时：

```bash
android-inspector start --serial 设备序列号
```

指定端口：

```bash
android-inspector start --port 8787
```

第二个必须实现的是 **命令行一键启动模块**。

连接 ADB 后命令行一键启动，做成 CLI 工具。用户只需要执行：

```bash
android-inspector start
```

它内部自动完成：

```text
1. 检查 adb
2. 检查 Android 设备
3. 连接 uiautomator2
4. 启动后端服务
5. 启动前端页面
6. 自动打开浏览器
7. 输出访问地址
```

控制台效果可以是：

```text
ADB 检测通过
发现设备：emulator-5554
uiautomator2 连接成功
服务已启动：http://127.0.0.1:8787
浏览器已打开
```

第三个必须实现的是 **屏幕截图与元素树同步模块**。

浏览器页面上显示的不是单纯截图，而是：

```text
手机截图 screenshot
+
当前页面 UI XML
+
设备真实分辨率
+
截图显示尺寸
```

因为后面你要支持“点截图获取元素”，必须做坐标换算。

例如手机真实分辨率是：

```text
1080 x 2400
```

浏览器里图片显示成：

```text
360 x 800
```

用户在浏览器里点击：

```text
x = 180, y = 400
```

需要换算成真实设备坐标：

```text
realX = 180 * 1080 / 360 = 540
realY = 400 * 2400 / 800 = 1200
```

然后再用这个坐标去匹配 XML 里的元素 `bounds`。

第四个必须实现的是 **鼠标点击截图获取元素模块**。

这是你第 1 点里的核心功能。逻辑应该是：

```text
用户点击浏览器截图
↓
前端拿到点击坐标
↓
换算成真实手机坐标
↓
后端遍历 UI XML 节点
↓
判断哪个节点的 bounds 包含该坐标
↓
返回最合适的元素
↓
右侧面板展示元素信息
```

匹配规则不能只取第一个元素。因为 Android UI XML 里经常是多个父子节点同时包含同一个坐标。正确做法是：

```text
1. 找到所有 bounds 包含点击坐标的节点
2. 优先选择面积最小的节点
3. 如果面积接近，优先选择有 resource-id / text / content-desc 的节点
4. 如果当前节点属性太少，允许用户切换父节点或子节点
```

元素树和截图点击要互相联动：

```text
点击截图上的元素
→ 元素树自动定位到对应 node

点击元素树上的 node
→ 截图上高亮对应 bounds
→ 右侧显示元素详情
```

第五个必须实现的是 **元素树查看与点击模块**。

左侧或者中间可以展示 XML 元素树。每个节点建议显示：

```text
class
resource-id
text
content-desc
bounds
clickable
enabled
```

例如：

```text
android.widget.Button
text=登录
resource-id=com.demo:id/login_btn
bounds=[120,680][960,760]
```

用户点击元素树节点后，需要：

```text
1. 截图区域高亮该元素边框
2. 右侧显示元素详情
3. 自动生成推荐定位器
4. 支持一键复制
```

这部分可以参考 Appium Inspector 的产品形态。Appium Inspector 本身就是用于移动应用可视化检查的 GUI 工具，支持展示应用截图、页面源码，并与应用交互；它还有 Recorder 功能，可以把 Inspector 中的操作录制成可执行代码。([GitHub][2])

第六个必须实现的是 **元素详情面板 + 一键复制模块**。

你说的“一边显示对应元素，支持一键复制”，建议右侧面板分成 4 块。

第一块：基础属性。

```text
text: 登录
resource-id: com.demo:id/login_btn
content-desc:
class: android.widget.Button
package: com.demo
bounds: [120,680][960,760]
clickable: true
enabled: true
selected: false
focused: false
```

第二块：推荐定位器。

```python
d(resourceId="com.demo:id/login_btn")
```

第三块：可执行代码。

```python
d(resourceId="com.demo:id/login_btn").click()
```

第四块：备用定位方式。

```python
d(text="登录")
d(className="android.widget.Button")
d.xpath('//*[@resource-id="com.demo:id/login_btn"]')
d.click(540, 720)
```

一键复制按钮建议至少有这些：

```text
复制 resource-id
复制 text
复制 XPath
复制 uiautomator2 定位器
复制点击代码
复制坐标
复制完整元素 JSON
```

第七个必须实现的是 **定位器生成模块**。

你不能只复制坐标。坐标只是兜底方案。推荐生成顺序应该是：

```text
1. resource-id
2. content-desc / description
3. text
4. resource-id + text
5. class + text
6. class + clickable
7. XPath
8. bounds
9. 坐标点击
```

生成示例：

```python
d(resourceId="com.demo:id/login_btn").click()
```

没有 resource-id 时：

```python
d(text="登录").click()
```

没有 text 时：

```python
d(description="login").click()
```

都没有时：

```python
d.xpath('//*[@class="android.widget.Button" and @clickable="true"]').click()
```

最后兜底：

```python
d.click(540, 720)
```

你可以在右侧显示一个“稳定性评分”：

```text
resource-id：稳定
content-desc：稳定
text：中等
XPath：一般
坐标：不稳定
```

第八个建议实现的是 **操作录制与脚本生成模块**。

虽然你现在只提到“获取元素来编写自动化测试脚本”，但实际二开时最好直接支持录制。否则用户每次还要手动复制很多代码。

录制功能可以支持：

```text
点击元素 → 生成 click
输入文本 → 生成 set_text
滑动屏幕 → 生成 swipe
返回键 → 生成 press("back")
等待元素 → 生成 wait
断言元素存在 → 生成 assert exists
```

最终导出：

```python
import uiautomator2 as u2

d = u2.connect()

d(resourceId="com.demo:id/login_btn").click()
d(resourceId="com.demo:id.username").set_text("test_user")
d(resourceId="com.demo:id.password").set_text("123456")
d(text="登录").click()

assert d(text="首页").exists
```

所以，你的 MVP 功能清单可以这样定：

| 模块     | 必须做什么                                |
| ------ | ------------------------------------ |
| ADB 连接 | 检测 adb、检测设备、连接当前手机、支持 serial         |
| 命令行启动  | `android-inspector start` 一键启动服务和浏览器 |
| 后端服务   | 提供截图、元素树、元素选择、代码生成接口                 |
| 前端页面   | 显示当前手机截图、元素树、元素详情                    |
| 坐标拾取   | 鼠标点击截图，根据坐标匹配元素                      |
| 元素树点击  | 点击树节点后高亮截图元素                         |
| 元素详情   | 显示 text、resource-id、class、bounds 等   |
| 一键复制   | 复制元素属性、定位器、点击代码、坐标                   |
| 定位器生成  | 自动生成 uiautomator2 / XPath / 坐标代码     |
| 刷新机制   | 页面变化后重新获取截图和 XML                     |

接口可以这样设计：

```text
GET /api/devices
获取当前 adb 设备列表

POST /api/connect
连接指定设备

GET /api/snapshot
获取截图、屏幕尺寸、UI XML

POST /api/select-by-coordinate
根据点击坐标查找元素

POST /api/select-by-node
根据元素树 nodeId 查找元素

POST /api/tap
执行真实点击

POST /api/input
输入文本

POST /api/generate-code
生成自动化代码

GET /api/export
导出测试脚本
```

前端页面建议用三栏布局：

```text
左侧：元素树
中间：手机截图
右侧：元素详情 + 定位器 + 一键复制
```

页面交互应该是：

```text
1. 打开浏览器后自动显示当前手机页面
2. 点击截图上的按钮
3. 截图上出现元素高亮框
4. 左侧元素树同步选中
5. 右侧显示元素属性
6. 点击“复制点击代码”
7. 得到 d(resourceId="xxx").click()
```

你的两个需求可以整理成正式开发需求：

```text
功能一：元素拾取与代码生成

用户可以在浏览器页面中查看当前 Android 手机截图和 UI 元素树。点击截图中的任意 UI 区域后，系统根据点击坐标匹配当前 UI XML 中的元素节点，并在右侧展示该元素的 text、resource-id、content-desc、class、bounds、clickable、enabled 等属性。同时，用户也可以点击左侧元素树中的任意节点，系统会在截图中高亮对应区域，并展示该节点详情。系统需要支持一键复制元素属性、推荐定位器、XPath、坐标以及自动化点击代码。

功能二：ADB 一键启动服务

用户在电脑连接 Android 设备并开启 USB 调试后，只需在命令行执行 android-inspector start，系统即可自动检测 adb 环境、识别当前连接设备、连接 uiautomator2 服务、启动本地后端服务，并自动打开浏览器页面。若存在多台设备，系统支持通过 --serial 参数指定设备；若设备未授权、离线或 adb 不存在，需要给出明确错误提示。
```

开发顺序建议：

```text
第一阶段：能启动
- adb devices 检测
- uiautomator2 connect
- FastAPI 服务启动
- 自动打开浏览器

第二阶段：能看
- 获取截图
- 获取 UI XML
- 前端显示截图
- 前端显示元素树

第三阶段：能选
- 点击截图获取坐标
- 坐标换算
- bounds 匹配元素
- 元素高亮
- 右侧展示属性

第四阶段：能复制
- 复制 text / resource-id / bounds
- 复制 uiautomator2 定位器
- 复制 click 代码
- 复制 XPath

第五阶段：能录制
- 记录 click / input / swipe
- 生成 pytest 脚本
- 导出 .py 文件
```

最小可用版本只要做到这 5 个点就可以：

```text
1. android-inspector start 一键启动
2. 浏览器显示当前手机截图
3. 左侧显示元素树
4. 鼠标点击截图后右侧显示元素信息
5. 一键复制定位器和点击代码
```

后续再加录制、导出脚本、自愈定位器、AI 推荐定位器、多设备管理。这样开发节奏比较稳，不会一开始就变成一个过重的 Appium Inspector 复制品。

[1]: https://github.com/openatx/uiautomator2/blob/master/README.md?utm_source=chatgpt.com "uiautomator2/README.md at master - GitHub"
[2]: https://github.com/appium/appium-inspector?utm_source=chatgpt.com "appium/appium-inspector: A GUI inspector for mobile apps ..."
